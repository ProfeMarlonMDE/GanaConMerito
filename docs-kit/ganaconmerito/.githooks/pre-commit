#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT"

echo "[pre-commit] Validando documentación y metadatos..."
python3 scripts/validate_docs.py

echo "[pre-commit] OK"
